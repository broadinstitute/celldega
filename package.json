{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.js --minify --format=esm --bundle --outdir=src/celldega/static"
	},
	"dependencies": {},
	"devDependencies": {
		"esbuild": "^0.20.0"
	}
}